
{{ form_start(form) }}

    <div class=" d-flex">

        {{ form_row(form.name) }}

        {{ form_row(form.cityImg) }}


    </div>
<button class="m-2 btn btn-primary">{{ button_label|default('Enregister') }}</button>

{{ form_end(form) }}
